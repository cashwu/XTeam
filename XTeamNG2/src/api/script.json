 [
    {
      "Name" : "test01",
      "Script" : "USE [Northwind]  GO  /****** Object:  StoredProcedure [dbo].[SalesByCategory]    Script Date: 2017/02/07 00:42:39 ******/  SET ANSI_NULLS ON  GO  SET QUOTED_IDENTIFIER ON  GO  ALTER PROCEDURE [dbo].[SalesByCategory]      @CategoryName nvarchar(15), @OrdYear nvarchar(4) = '1998'  AS  IF @OrdYear != '1996' AND @OrdYear != '1997' AND @OrdYear != '1998'  BEGIN   SELECT @OrdYear = '1998'  END    SELECT ProductName,   TotalPurchase=ROUND(SUM(CONVERT(decimal(14,2), OD.Quantity * (1-OD.Discount) * OD.UnitPrice)), 0)  FROM [Order Details] OD, Orders O, Products P, Categories C  WHERE OD.OrderID = O.OrderID    AND OD.ProductID = P.ProductID    AND P.CategoryID = C.CategoryID   AND C.CategoryName = @CategoryName   AND SUBSTRING(CONVERT(nvarchar(22), O.OrderDate, 111), 1, 4) = @OrdYear  GROUP BY ProductName  ORDER BY ProductName"
    },
    {
      "Name" : "test02",
      "Script" : "USE [Northwind]  GO  /****** Object:  StoredProcedure [dbo].[SalesByCategory]    Script Date: 2017/02/07 00:42:39 ******/  SET ANSI_NULLS ON  GO  SET QUOTED_IDENTIFIER ON  GO  ALTER PROCEDURE [dbo].[SalesByCategory]      @CategoryName nvarchar(15), @OrdYear nvarchar(4) = '1998'  AS  IF @OrdYear != '1996' AND @OrdYear != '1997' AND @OrdYear != '1998'  BEGIN   SELECT @OrdYear = '1998'  END    SELECT ProductName,   TotalPurchase=ROUND(SUM(CONVERT(decimal(14,2), OD.Quantity * (1-OD.Discount) * OD.UnitPrice)), 0)  FROM [Order Details] OD, Orders O, Products P, Categories C  WHERE OD.OrderID = O.OrderID    AND OD.ProductID = P.ProductID    AND P.CategoryID = C.CategoryID   AND C.CategoryName = @CategoryName   AND SUBSTRING(CONVERT(nvarchar(22), O.OrderDate, 111), 1, 4) = @OrdYear  GROUP BY ProductName  ORDER BY ProductName"
    },
    {
      "Name" : "test03",
      "Script" : "USE [Northwind]  GO  /****** Object:  StoredProcedure [dbo].[SalesByCategory]    Script Date: 2017/02/07 00:42:39 ******/  SET ANSI_NULLS ON  GO  SET QUOTED_IDENTIFIER ON  GO  ALTER PROCEDURE [dbo].[SalesByCategory]      @CategoryName nvarchar(15), @OrdYear nvarchar(4) = '1998'  AS  IF @OrdYear != '1996' AND @OrdYear != '1997' AND @OrdYear != '1998'  BEGIN   SELECT @OrdYear = '1998'  END    SELECT ProductName,   TotalPurchase=ROUND(SUM(CONVERT(decimal(14,2), OD.Quantity * (1-OD.Discount) * OD.UnitPrice)), 0)  FROM [Order Details] OD, Orders O, Products P, Categories C  WHERE OD.OrderID = O.OrderID    AND OD.ProductID = P.ProductID    AND P.CategoryID = C.CategoryID   AND C.CategoryName = @CategoryName   AND SUBSTRING(CONVERT(nvarchar(22), O.OrderDate, 111), 1, 4) = @OrdYear  GROUP BY ProductName  ORDER BY ProductName"
    },
    {
      "Name" : "test04",
      "Script" : "USE [Northwind]  GO  /****** Object:  StoredProcedure [dbo].[SalesByCategory]    Script Date: 2017/02/07 00:42:39 ******/  SET ANSI_NULLS ON  GO  SET QUOTED_IDENTIFIER ON  GO  ALTER PROCEDURE [dbo].[SalesByCategory]      @CategoryName nvarchar(15), @OrdYear nvarchar(4) = '1998'  AS  IF @OrdYear != '1996' AND @OrdYear != '1997' AND @OrdYear != '1998'  BEGIN   SELECT @OrdYear = '1998'  END    SELECT ProductName,   TotalPurchase=ROUND(SUM(CONVERT(decimal(14,2), OD.Quantity * (1-OD.Discount) * OD.UnitPrice)), 0)  FROM [Order Details] OD, Orders O, Products P, Categories C  WHERE OD.OrderID = O.OrderID    AND OD.ProductID = P.ProductID    AND P.CategoryID = C.CategoryID   AND C.CategoryName = @CategoryName   AND SUBSTRING(CONVERT(nvarchar(22), O.OrderDate, 111), 1, 4) = @OrdYear  GROUP BY ProductName  ORDER BY ProductName"
    },
    {
      "Name" : "test05",
      "Script" : "USE [Northwind]  GO  /****** Object:  StoredProcedure [dbo].[SalesByCategory]    Script Date: 2017/02/07 00:42:39 ******/  SET ANSI_NULLS ON  GO  SET QUOTED_IDENTIFIER ON  GO  ALTER PROCEDURE [dbo].[SalesByCategory]      @CategoryName nvarchar(15), @OrdYear nvarchar(4) = '1998'  AS  IF @OrdYear != '1996' AND @OrdYear != '1997' AND @OrdYear != '1998'  BEGIN   SELECT @OrdYear = '1998'  END    SELECT ProductName,   TotalPurchase=ROUND(SUM(CONVERT(decimal(14,2), OD.Quantity * (1-OD.Discount) * OD.UnitPrice)), 0)  FROM [Order Details] OD, Orders O, Products P, Categories C  WHERE OD.OrderID = O.OrderID    AND OD.ProductID = P.ProductID    AND P.CategoryID = C.CategoryID   AND C.CategoryName = @CategoryName   AND SUBSTRING(CONVERT(nvarchar(22), O.OrderDate, 111), 1, 4) = @OrdYear  GROUP BY ProductName  ORDER BY ProductName"
    },
    {
      "Name" : "test06",
      "Script" : " select * from Customer 06"
    },
    {
      "Name" : "test07",
      "Script" : " select * from Customer 07"
    },
    {
      "Name" : "test08",
      "Script" : " select * from Customer 08"
    },
    {
      "Name" : "test09",
      "Script" : " select * from Customer 09"
    },
    {
      "Name" : "test10",
      "Script" : " select * from Customer 10"
    },
    {
      "Name" : "test11",
      "Script" : " select * from Customer 11"
    },
    {
      "Name" : "test12",
      "Script" : " select * from Customer 12"
    }
  ]
